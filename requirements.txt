# Runtime + test deps, pinned to the versions this app was built and verified against.
# (Custom clocks use fixed UTC offsets, so no zoneinfo/tzdata dependency is needed.)
textual==8.2.8
rich==15.0.0
pytest==9.1.1
pytest-asyncio==1.4.0
